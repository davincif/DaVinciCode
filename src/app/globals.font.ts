import { Permanent_Marker, Poppins } from 'next/font/google'

export const stdOrdinaryFont = Poppins({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
})

export const stdRadiantFont = Permanent_Marker({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
})
